apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ldap-users-pvc
  namespace: slurm-cluster
  labels:
    app: slurm-login
spec:
  volumeName: ldap-users-pv
  accessModes: [ReadWriteMany]
  storageClassName: ''
  resources:
    requests:
      storage: 1000Gi
